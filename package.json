{
  "name": "belzabar",
  "private": true,
  "workspaces": [
    "cli",
    "extension",
    "packages/*"
  ],
  "scripts": {
    "install": "bash ./scripts/install.sh",
    "reinstall": "bash ./scripts/reinstall.sh",
    "belz:build": "bun run --cwd cli build",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.8.3"
  }
}
