*Most Important*: 
- Include Claude-Code (maybe using Claude-code-acp adapter)
- For worker agents: have multiple contracts and easily get main-orchestrator agent to use those contractor workers.
- Why can't I resume previously used chat session? For This I need to understand who the ACP works, what does it means by a session acc. to the ACP. how is it being determined if  the session is active or closed?

- Integrate simple teamwork apis, to get tasks, their comments, create comments etc, may change a few things there as well. (Big Work, but could be really useful) 

Additional nice to have features (mostly related to extensions);
1. Use an API execution curl to open an AD Page, with autofilled inputs.
2. Change draft to published (with the same inputs)
3. Copy AD Link: with display value: SERVICE_CATEGORY->AD_NAME: `UUID` but link value as the actual link;


