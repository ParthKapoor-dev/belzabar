{
  "name": "pd-cli",
  "module": "index.ts",
  "type": "module",
  "bin": {
    "pd": "./bin/pd.ts"
  },
  "scripts": {
    "generate": "bun run utils/generate-registry.ts",
    "build": "bun run generate && bun build --compile --minify --sourcemap ./bin/pd-build.ts --outfile pd",
    "install-cli": "bun run build && mkdir -p ~/.local/bin && mv pd ~/.local/bin/pd"
  },
  "private": true,
  "dependencies": {
    "@belzabar/core": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
